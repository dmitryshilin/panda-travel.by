- if @random_tours.present?
  #some-tours
    %ul.also-slider
      - @random_tours.each do |random|
        %li=link_to image_tag(random.images.first.image.url(:medium), title: random.title), tour_path(random), title: random.title