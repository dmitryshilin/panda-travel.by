- if @random_tours.present?
  .also
    %h2 Смотрите также
    %ul.also-slider
      - @random_tours.each do |random|
        %li
          =link_to image_tag(random.images.first.image.url(:medium), title: "#{link_to random.short_title, tour_path(random)}"), tour_path(random), title: random.short_title
