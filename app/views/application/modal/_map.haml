- @obj
.modal-dialog
  .modal-content
    .modal-header
      %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
      %h4.modal-title Карта тура
      .modal-body
        #gmap{ data: { role: "page" } }
          = @obj.waypoints.inspect
          = gmaps({ scripts: :none, direction: { data: @obj.start_end, options: { waypoints: @obj.waypoints } } })
          = yield :scripts
      .modal-footer
        %button.btn{"data-dismiss" => "modal", type: "button"} Закрыть
